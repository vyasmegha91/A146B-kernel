- name: Build Kernel
      run: |
        export ARCH=arm64
        cd kernel-source
        echo "=== Defconfig files ==="
        find . -name "*defconfig*" | head -30
        echo "=== arch/arm64/configs ==="
        ls arch/arm64/configs/ | head -30
        
